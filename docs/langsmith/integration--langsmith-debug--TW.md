# LangSmith Debug 整合指南

ElevenDops LangGraph 工作流程的 LangSmith 除錯功能完整指南。

## 概述

LangSmith Debug 整合為 ElevenDops 腳本生成工作流程提供全面的除錯、追蹤和視覺化功能。此整合讓開發者能夠使用 LangSmith 強大的除錯工具來監控、除錯和優化 LangGraph 工作流程執行。

## 功能特色

### ✅ **已實現功能**

- **🔍 全面追蹤**：自動擷取所有工作流程步驟的追蹤資料
- **🎯 Debug API 端點**：用於除錯工作流程執行的專用端點
- **📊 工作階段管理**：分組和管理除錯工作階段
- **🛡️ 優雅降級**：當 LangSmith 不可用時系統持續運作
- **⚙️ 可設定的追蹤層級**：Debug、info 和 error 追蹤層級
- **🏠 本地 Studio 環境**：用於本地除錯的 Node.js 工具
- **🧪 基於屬性的測試**：53 個通過測試的完整測試覆蓋

## 快速開始

### 1. 環境設定

將 LangSmith 設定加入您的 `.env` 檔案：

```bash
# LangSmith 設定
LANGCHAIN_API_KEY=your_langsmith_api_key_here
LANGCHAIN_PROJECT=elevendops-langgraph-debug
LANGSMITH_TRACING=true
LANGSMITH_TRACE_LEVEL=info
```

### 2. 啟動後端

```bash
uv run uvicorn backend.main:app --reload
```

### 3. 驗證整合

檢查除錯健康檢查端點：

```bash
curl http://localhost:8000/api/debug/health
```

預期回應：

```json
{
  "status": "healthy",
  "langsmith_configured": true,
  "langsmith_available": true,
  "project": "elevendops-langgraph-debug",
  "trace_level": "info"
}
```

### 4. 啟動本地 Studio（選用）

```bash
cd langsmith-studio
pnpm run dev
```

## Debug API 參考

### 健康檢查

```http
GET /api/debug/health
```

回傳 LangSmith 整合狀態和設定。

### 執行除錯腳本生成

```http
POST /api/debug/script-generation
```

**請求內容：**

```json
{
  "knowledge_content": "醫學知識內容",
  "prompt": "生成關於糖尿病照護的腳本",
  "model_name": "gemini-2.0-flash",
  "debug_level": "info",
  "session_name": "diabetes-debug-session"
}
```

**回應：**

```json
{
  "trace_id": "trace_abc123",
  "session_id": "session_xyz789",
  "execution_status": "completed",
  "generated_script": "生成的腳本內容...",
  "steps": [
    {
      "step_id": "step_1",
      "node_name": "prepare_context",
      "start_time": "2024-12-24T10:00:00Z",
      "end_time": "2024-12-24T10:00:05Z",
      "duration_ms": 5000,
      "error": null
    }
  ],
  "total_duration_ms": 15000,
  "langsmith_url": "https://smith.langchain.com/o/default/projects/p/elevendops-langgraph-debug"
}
```

### 工作階段管理

#### 建立除錯工作階段

```http
POST /api/debug/sessions
```

**請求內容：**

```json
{
  "name": "我的除錯工作階段"
}
```

#### 列出除錯工作階段

```http
GET /api/debug/sessions
```

#### 取得工作階段詳情

```http
GET /api/debug/sessions/{session_id}
```

#### 結束除錯工作階段

```http
POST /api/debug/sessions/{session_id}/end
```

## 設定選項

### 環境變數

| 變數                    | 預設值                       | 說明                               |
| ----------------------- | ---------------------------- | ---------------------------------- |
| `LANGCHAIN_API_KEY`     | -                            | LangSmith API 金鑰（追蹤必需）     |
| `LANGCHAIN_PROJECT`     | `elevendops-langgraph-debug` | LangSmith 專案名稱                 |
| `LANGSMITH_TRACING`     | `true`                       | 啟用/停用 LangSmith 追蹤           |
| `LANGSMITH_TRACE_LEVEL` | `info`                       | 追蹤詳細程度（debug、info、error） |

### 追蹤層級

- **`debug`**：擷取所有工作流程資料，包括輸入/輸出狀態
- **`info`**：擷取基本工作流程資訊和時間
- **`error`**：僅擷取錯誤資訊和堆疊追蹤

## 本地 Studio 環境

`langsmith-studio` 目錄包含用於本地除錯的 Node.js 工具：

### 設定

```bash
cd langsmith-studio
pnpm install
```

### 使用方式

```bash
# 檢查後端連線和 LangSmith 狀態
pnpm run dev

# 僅測試連線
pnpm run test
```

### 設定檔

編輯 `langsmith-studio/src/studio.config.js`：

```javascript
export const studioConfig = {
  backend: {
    url: "http://localhost:8000",
    debugEndpoint: "/api/debug",
    healthEndpoint: "/api/debug/health",
    timeout: 30000,
  },
  langsmith: {
    apiKey: process.env.LANGCHAIN_API_KEY,
    project: "elevendops-langgraph-debug",
    baseUrl: "https://smith.langchain.com",
  },
  debug: {
    traceLevel: "info",
    logRequests: false,
  },
};
```

## 架構

### 元件

```
┌─────────────────────────────────────────────────────────────────────┐
│                    LangSmith Debug 整合                               │
│                                                                       │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐  │
│  │  Debug API       │  │  LangSmith       │  │  Local Studio    │  │
│  │  端點            │  │  追蹤服務        │  │  環境            │  │
│  └────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘  │
│           │                     │                     │             │
└───────────┼─────────────────────┼─────────────────────┼─────────────┘
            │                     │                     │
┌───────────▼─────────────────────▼─────────────────────▼─────────────┐
│                    增強型 LangGraph 工作流程                          │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  追蹤工作流程節點                                             │   │
│  │  - prepare_context（含追蹤）                                  │   │
│  │  - generate_script（含追蹤）                                  │   │
│  │  - post_process（含追蹤）                                     │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                       │
└───────────────────────────────────────┬───────────────────────────────┘
                                        │
┌───────────────────────────────────────▼───────────────────────────────┐
│                         LangSmith 雲端                                 │
│                                                                         │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐    │
│  │  追蹤資料庫      │  │  專案：          │  │  網頁儀表板      │    │
│  │                  │  │  elevendops-     │  │                  │    │
│  │                  │  │  langgraph-debug │  │                  │    │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘    │
└─────────────────────────────────────────────────────────────────────┘
```

### 資料流程

1. **除錯請求** → Debug API 接收帶有工作流程參數的請求
2. **建立工作階段** → 選擇性建立用於分組追蹤的除錯工作階段
3. **工作流程執行** → 增強型 LangGraph 工作流程與追蹤一起執行
4. **追蹤擷取** → 擷取每個工作流程步驟的時間和狀態資料
5. **LangSmith 上傳** → 追蹤資料發送到 LangSmith 雲端（如果可用）
6. **回應** → 回傳帶有追蹤 ID 和執行詳情的除錯回應
7. **視覺化** → 追蹤可在 LangSmith 儀表板或本地 Studio 中查看

## 測試

### 執行測試

```bash
# 執行所有 LangSmith 相關測試
uv run python -m pytest tests/ -k "langsmith" -v

# 執行 debug API 測試
uv run python -m pytest tests/ -k "debug" -v

# 執行特定測試檔案
uv run python -m pytest tests/test_langsmith_tracer_props.py -v
```

### 測試覆蓋率

- **29 個 LangSmith 追蹤器測試** ✅
- **24 個 Debug API 測試** ✅
- 所有正確性屬性的**基於屬性的測試**
- LangSmith 不可用時的**優雅降級測試**
- 除錯工作階段管理的**工作階段持久性測試**

## 疑難排解

### 常見問題

#### 1. LangSmith API 金鑰未設定

**症狀**：健康檢查中 `langsmith_configured: false`

**解決方案**：在您的 `.env` 檔案中設定 `LANGCHAIN_API_KEY`

#### 2. LangSmith 服務不可用

**症狀**：健康檢查中 `langsmith_available: false`

**解決方案**：檢查網路連線和 API 金鑰有效性。系統會優雅降級。

#### 3. 追蹤未出現在 LangSmith 中

**症狀**：Debug API 運作但 LangSmith 儀表板中無追蹤

**解決方案**：

- 驗證 `LANGCHAIN_PROJECT` 與您的 LangSmith 專案匹配
- 檢查 API 金鑰權限
- 確保 `LANGSMITH_TRACING=true`

#### 4. 本地 Studio 連線失敗

**症狀**：Studio 顯示「後端未連線」

**解決方案**：

- 確保後端在 `http://localhost:8000` 上執行
- 檢查防火牆設定
- 驗證除錯端點可存取

### 除錯指令

```bash
# 檢查 LangSmith 設定
uv run python -c "
from backend.config import get_settings
settings = get_settings()
print(f'LangSmith configured: {settings.is_langsmith_configured()}')
print(f'Project: {settings.langsmith_project}')
print(f'Trace level: {settings.langsmith_trace_level}')
"

# 測試追蹤器服務
uv run python -c "
from backend.services.langsmith_tracer import get_tracer
tracer = get_tracer()
print(f'Tracer available: {tracer.is_available()}')
session_id = tracer.start_trace_session('test-session')
print(f'Test session created: {session_id}')
"

# 測試 debug API
curl -X POST http://localhost:8000/api/debug/script-generation \
  -H "Content-Type: application/json" \
  -d '{
    "knowledge_content": "測試內容",
    "prompt": "測試提示"
  }'
```

## 最佳實踐

### 1. 工作階段管理

- 使用描述性工作階段名稱以便識別
- 將相關的除錯執行分組在同一工作階段中
- 當除錯完成時結束工作階段以釋放資源

### 2. 追蹤層級

- 使用 `debug` 層級進行詳細疑難排解
- 使用 `info` 層級進行一般監控
- 在生產環境中使用 `error` 層級以最小化開銷

### 3. 效能考量

- 追蹤增加的開銷很小（每個工作流程約 50-100 毫秒）
- Debug 層級追蹤擷取更多資料但使用更多頻寬
- 工作階段儲存在記憶體中；結束未使用的工作階段

### 4. 安全性

- 保持 LangSmith API 金鑰安全
- 不要將 API 金鑰提交到版本控制
- 為不同部署使用環境特定的專案

## 與現有工作流程整合

LangSmith 整合設計為非侵入性：

- 現有工作流程邏輯**無需變更**
- 當 LangSmith 已設定時**自動追蹤**
- 當 LangSmith 不可用時**優雅降級**
- 用於增強除錯的**選用除錯端點**

## 未來增強

### 計畫功能

- 透過 WebSocket 的**即時追蹤串流**
- Debug API 中的**進階篩選**
- **追蹤比較**工具
- **效能分析**儀表板
- **自訂追蹤註解**

### 整合機會

- 用於自動化測試的 **CI/CD 管線整合**
- 用於工作流程失敗的**警報系統**
- **效能監控**儀表板
- 用於工作流程優化的 **A/B 測試**框架

## 支援

### 文件

- [LangSmith 官方文件](https://docs.smith.langchain.com/)
- [LangGraph 文件](https://langchain-ai.github.io/langgraph/)
- [ElevenDops 架構指南](./MVP1_ARCHITECTURE.md)

### 取得協助

1. 首先查看此文件
2. 查看測試檔案以取得使用範例
3. 檢查 LangSmith 儀表板以取得追蹤詳情
4. 使用除錯健康檢查端點驗證設定

---

**最後更新**：2024 年 12 月 26 日
**整合狀態**：✅ 完成並已測試
**測試覆蓋率**：53 個通過測試
